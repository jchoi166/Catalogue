import { Fragment } from "react";

const Login = (props) => {
  return <Fragment>
     <p>Login</p>
     {/* <p>Welcome {props.user}!</p>   User's name will go here, pulled from AuthCtx */}
  </Fragment>;
};

export default Login;
